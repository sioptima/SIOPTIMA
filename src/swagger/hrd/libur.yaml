paths:
  /api/hrd/libur:
    get:
      parameters:
      - in: query
        name: page
        schema:
          type: integer
          description: Which page to show
      - in: query
        name: limit
        schema:
          type: integer
          description: Number of record to return
      summary: Get all users day off request
      tags:
        - Libur

      responses:
        '200':
          description: Day off history
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: Records retrieved
                  data:
                    type: array
                    items:
                      type: object
                      properties:

  /api/hrd/libur/{id}:
    get:
      parameters:
        - in: path
          name: id
          schema:
            type: integer
            description: Record's id to search
      summary: Get a day off request record
      tags:
        - Libur

      responses:
        '200':
          description: Record information
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: Record retrieved
                  data:
                    type: object
                    properties:

  /api/hrd/libur/{id}/approve:
    patch:
      parameters:
        - in: path
          name: id
          schema:
            type: integer
            description: Record's id to approve
      summary: Approve a dayoff request
      tags:
        - Libur

      responses:
        '200':
          description: Request confirmation
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: Record retrieved
                  data:
                    type: object
                    properties:

  /api/hrd/libur/{id}/reject:
    patch:
      parameters:
        - in: path
          name: id
          schema:
            type: integer
            description: Record's id to reject
      summary: Reject a dayoff request
      tags:
        - Libur

      responses:
        '200':
          description: Request confirmation
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: Record retrieved
                  data:
                    type: object
                    properties: