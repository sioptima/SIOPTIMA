JWT_SECRET= "randomstringhere"
INIT_ADMIN_PASSWORD = "randomstringhere"
INIT_OPERATOR_PASSWORD = "randomstringhere"
INIT_HRD_PASSWORD = "randomstringhere"


# Connect to Supabase via connection pooling
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?SCHEMA"

# Direct connection to the database. Used for migrations
DIRECT_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?SCHEMA"
